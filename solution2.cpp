#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    long long n, m, a, b;
    cin >> n >> m >> a >> b;
    
    long long cost = n * a;
    
    cout << cost << endl;
    return 0;
}